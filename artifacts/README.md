# Artifacts

Pictures and sound the benches and the MAME probes produce, kept where the
person following along can look and listen: `model/` (the reference
renderer), `rtl/` (the gateware's frames), `audio/` (WAVs are ignored;
the comparison report is kept). It bisects — METHODOLOGY section 5.15.
